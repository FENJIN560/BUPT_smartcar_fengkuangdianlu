#include "zf_common_headfile.h"

// ------------------------------------------------------------------
// 系统毫秒计数器
// ------------------------------------------------------------------
static volatile uint32 g_system_ms = 0;

// ------------------------------------------------------------------
// 每 1ms 定时器中断回调（PIT或SysTick）
// ------------------------------------------------------------------
IFX_INTERRUPT(sys_tick_isr, 0, 0)
{

    // 如果使用 PIT，需要清除标志：
     pit_clear_flag(CCU60_CH0);
}

// ------------------------------------------------------------------
// 返回系统时间（毫秒）
// ------------------------------------------------------------------
uint32 system_get_time(void)
{
    return g_system_ms;
}

// ------------------------------------------------------------------
// 初始化系统定时器
// ------------------------------------------------------------------
void system_timer_init(void)
{
    // 配置 PIT 或 SysTick 每 1ms 触发
    // 例如：
    // pit_ms_init(CCU60_CH0, 1);    // 每 1ms
    // interrupt_enable(CCU60_CH0);  // 使能中断
}

